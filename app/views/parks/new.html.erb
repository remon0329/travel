<% content_for :title, "公園を登録する" %>

<h1 class="text-2xl font-bold text-center my-6">
  <%= t('views.park.new.title') %>
</h1>

<div class="max-w-xl mx-auto">
  <%= render "form", park: @park %>

  <div class="text-center mt-6">
    <%= link_to "投稿一覧に戻る", parks_path, class: "text-blue-600 hover:underline" %>
  </div>
</div>
