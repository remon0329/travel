<%= form_with(model: park, local: true, html: { multipart: true }) do |form| %>
  <% if park.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(park.errors.count, "error") %> prohibited this park from being saved:</h2>

      <ul>
        <% park.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :公園名, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :説明欄, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :住所, style: "display: block" %>
    <%= form.text_field :address %>
  </div>

  <div>
    <%= form.label :写真 %>
    <%= form.file_field :image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
